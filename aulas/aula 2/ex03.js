const somar = (a , b ) = > {
return a + b ;
console.log(`soma: ${num1 + num2}`);
};

const subtrair = (a , b ) = > {
return a - b ;
console.log(`soma: ${num1 - num2}`);
};

const multiplicar = (a , b ) = > {
return a  * b ;
console.log(`soma: ${num1 * num2}`);
};

const dividir = (a , b ) = > {
return a / b ;
console.log(`soma: ${num1 / num2}`);
};
